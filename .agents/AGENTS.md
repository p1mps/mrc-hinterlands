# Workspace Rules

## Code Display & Diffs
- Always output code diffs (` ```diff ` blocks showing removed lines with `-` and added lines with `+`) in chat messages and walkthrough artifacts whenever editing or modifying files.
