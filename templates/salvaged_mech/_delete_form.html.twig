<form method="post" action="{{ path('app_salvaged_mech_delete', {'id': salvaged_mech.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salvaged_mech.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
